<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@high_score, :html => { :role => "form" }) do |f| %>
        <% if @high_score.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@high_score.errors.count, "error") %> prohibited this high_score from being saved:</h4>

            <ul>
            <% @high_score.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.label :game %>
        <%= f.text_field :game, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :score %>
        <%= f.number_field :score, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :user %>
        <%= f.text_field :user, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 