<%= form_for(board_group, :local=>true, :url=>[:admin, board_group], :html => { :role => "form" }) do |f| %>

  <% if board_group.errors.any? %>
  <div class="alert alert-danger alert-dismissable" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
    <h4><%= pluralize(board_group.errors.count, "error") %> prohibited this board_group from being saved:</h4>

    <ul>
    <% board_group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="form-inputs">

    <div class="form-group">
      <%= f.label :bgid %>
      <%= f.text_field :bgid, :class => "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, :class => "form-control" %>
    </div>

    <div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
    </div>

  </div>

<% end %>



